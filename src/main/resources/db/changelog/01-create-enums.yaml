databaseChangeLog:
  - changeSet:
      id: v1-create-enums
      author: ediel96
      changes:
        - sql:
            dbms: postgresql
            sql: CREATE TYPE transaction_type AS ENUM ('income', 'expense');
      rollback:
        - sql:
            dbms: postgresql
            sql: DROP TYPE IF EXISTS transaction_type;